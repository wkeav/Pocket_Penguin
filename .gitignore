# Django 
__pycache__/
*.py[cod]
*$py.class 

# Django packaging 
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

.npm
.vscode-test
.DS_Store
.AppleDouble
.LSOverride

#IDE files
.vscode/settings.json
.vscode/launch.json
.vscode/extensions.json

# Allow tracking of the Flutter app `lib/` directory inside the frontend folder.
# The top-level `.gitignore` intentionally ignores generic `lib/` folders
# used by Python packaging, but we need to include the Flutter `lib/`
# under `frontend/pocket_penguin_app` so Dart/Flutter sources can be tracked.
!frontend/pocket_penguin_app/lib/
!frontend/pocket_penguin_app/lib/**